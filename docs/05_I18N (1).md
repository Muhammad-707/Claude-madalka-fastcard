# 05 — i18n (react-i18next)

Языки: **ru** (по умолчанию), **en**, **tj**. Все строки — через `t()`. Хардкод запрещён.

## Настройка → `src/i18n/index.ts`
```ts
import i18n from 'i18next'
import { initReactI18next } from 'react-i18next'
import LanguageDetector from 'i18next-browser-languagedetector'
import ru from './locales/ru.json'; import en from './locales/en.json'; import tj from './locales/tj.json'

i18n.use(LanguageDetector).use(initReactI18next).init({
  resources: { ru:{translation:ru}, en:{translation:en}, tj:{translation:tj} },
  fallbackLng: import.meta.env.VITE_DEFAULT_LANG ?? 'ru',
  interpolation: { escapeValue: false },
})
export default i18n
```
Импортировать `@/i18n` в `main.tsx` до рендера.

## Ключи (namespace через точку)
```jsonc
{
  "auth":      { "welcome":"...", "login":"...", "email":"...", "password":"...", "forgot":"...", "onlyAdmin":"..." },
  "nav":       { "dashboard":"...", "orders":"...", "products":"...", "other":"...", "categories":"...", "brands":"...", "banners":"...", "logout":"..." },
  "dashboard": { "products":"...", "customers":"...", "categoriesBrands":"...", "topSelling":"...", "seeAll":"...", "recentCustomers":"...", "topByUnits":"..." },
  "users":     { "title":"...", "addUser":"...", "search":"...", "role":"...", "deleteSelected":"...", "username":"...", "email":"..." },
  "products":  { "title":"...", "addNew":"...", "edit":"...", "delete":"...", "price":"...", "quantity":"...", "code":"...", "discount":"..." },
  "form":      { "save":"...", "cancel":"...", "name":"...", "description":"...", "image":"...", "color":"...", "newColor":"...", "category":"...", "subCategory":"...", "brand":"..." },
  "category":  { "title":"...", "addCategory":"...", "categoryName":"..." },
  "brand":     { "title":"...", "addBrand":"...", "brandName":"..." },
  "banner":    { "title":"...", "addBanner":"..." },
  "common":    { "confirmDelete":"...", "yes":"...", "no":"...", "successAdded":"...", "successUpdated":"...", "successDeleted":"..." },
  "empty":     { "noProducts":"...", "noOrders":"...", "noData":"..." },
  "errors":    { "network":"...", "notFound":"...", "required":"...", "invalidEmail":"...", "loginFailed":"..." }
}
```
Три файла `ru.json`/`en.json`/`tj.json` с одинаковыми ключами (tj — таджикский, кириллица). Переключение — `i18n.changeLanguage(code)` без перезагрузки.
